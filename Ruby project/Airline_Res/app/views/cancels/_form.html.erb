<%= form_for(cancel) do |f| %>
  <% if cancel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cancel.errors.count, "error") %> prohibited this cancel from being saved:</h2>

      <ul>
      <% cancel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :E_Tickit_No %>
    <%= f.text_field :E_Tickit_No %>
  </div>

  <div class="field">
    <%= f.label :integer %>
    <%= f.text_field :integer %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
